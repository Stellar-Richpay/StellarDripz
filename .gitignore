# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/

# Environment files
.env.local
.env*.local

# Rust / Cargo build artifacts
contracts/target/
**/*.rs.bk

# Runtime data — the in-memory DB fallback writes a JSON store here
# containing transaction records with IPs/user agents; it must never
# be committed or deployed.
/data/

# Database (JSON dump files — keep .gitkeep for directory tracking)
data/*.json

# Turbo
.turbo/

# Playwright
test-results/
playwright-report/

# Jest coverage
coverage/

# Rust WASM build artifacts (contract compilation)

# Video pipeline scratch space. Holds rendered frame dumps, intermediate
# encodes, and the throwaway testnet keypair the capture harness signs with —
# none of it may be committed (the keypair is still a private key).
/video/.work/

